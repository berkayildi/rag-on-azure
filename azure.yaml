# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: rag-on-azure
metadata:
  template: rag-on-azure@0.0.1

services:
  app:
    project: ./app
    language: python
    host: containerapp

hooks: {}
